<?xml version="1.0"?>
<!--
  ~ Licensed to the Apache Software Foundation (ASF) under one or more
  ~ contributor license agreements.  See the NOTICE file distributed with
  ~ this work for additional information regarding copyright ownership.
  ~ The ASF licenses this file to you under the Apache License, Version 2.0
  ~ (the "License"); you may not use this file except in compliance with
  ~ the License.  You may obtain a copy of the License at
  ~
  ~ http://www.apache.org/licenses/LICENSE-2.0
  ~
  ~ Unless required by applicable law or agreed to in writing, software
  ~ distributed under the License is distributed on an "AS IS" BASIS,
  ~ WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  ~ See the License for the specific language governing permissions and
  ~ limitations under the License.
  -->

<document>
<properties>
  <title>BreakTest Features And Differences</title>
</properties>
<body>

<section name="BreakTest Features And Differences">
<p>
This is the BreakTest manual. It documents the features and behavioral changes
that are specific to BreakTest 2026.07.11. For the inherited JMeter-compatible
test-plan model, use the official Apache JMeter documentation.
</p>

<h2>Runtime Baseline</h2>
<ul>
  <li>Java 21 or later is required.</li>
  <li>Virtual threads are used where they reduce thread overhead.</li>
  <li>Legacy BeanShell, BSF/JEXL2, Rhino JavaScript, and LogKit paths were removed from the modern scripting/runtime surface.</li>
  <li>JMeter-compatible names are retained where they are part of file formats, APIs, plugins, scripts, or automation.</li>
</ul>

<h2>Modern HTTP And HTTP/2</h2>
<ul>
  <li>The built-in HTTP sampler path uses Apache HttpClient 5.</li>
  <li>HTTP/2 and HTTP/2-preferred protocol selection are available through the async HttpClient 5 path.</li>
  <li>BreakTest handles negotiated fallback, HTTP/2 upload content length, connect-time reporting, and safer replay behavior.</li>
  <li>HTTP/2 resource usage was reduced by limiting reactor threads and avoiding unnecessary client/reactor rebuilds between same-user iterations.</li>
  <li>NTLM authentication was fixed on the HttpClient 5 path, including variable resolution before async authentication callbacks.</li>
  <li>Brotli and Zstandard response decoding were added alongside gzip and deflate.</li>
</ul>

<h2>Lower Runtime Resource Usage</h2>
<ul>
  <li>Lazy response decompression stores compressed bytes and decompresses only when response data is accessed.</li>
  <li>Response retention modes can keep compressed data, discard successful bodies, keep only failing bodies, or retain MD5 checksums.</li>
  <li>Lightweight test-plan cloning reduces per-thread object churn.</li>
  <li>Header merging, temporary property handling, response read buffers, and diagnostic metadata paths were optimized.</li>
  <li>Rich sample diagnostics are lazy and listener-driven, so normal load runs avoid unnecessary per-sample storage.</li>
</ul>

<h2>Unified Scheduling</h2>
<p>
BreakTest moves load modeling into a unified Thread Group rather than splitting
common scenarios across several specialized thread groups.
</p>
<ul>
  <li>Closed-model schedules can combine different ramp-up speeds, hold periods, and target thread counts.</li>
  <li>Open-model schedules can model arrivals, use even-arrival helpers, and cap active threads.</li>
  <li>Thread Group pacing controls are available for transaction pacing.</li>
  <li>Schedule parsing is safer and includes graph preview support.</li>
  <li>Runtime user variables are cleared on new iterations when same-user mode is disabled, while initial variables are preserved.</li>
</ul>

<h2>Pause, Resume, And Faster Shutdown</h2>
<ul>
  <li>Pause and Resume are available in the GUI and through <code>bin/pause.*</code> and <code>bin/resume.*</code>.</li>
  <li>In closed model, pause holds active threads and stops further ramp-up.</li>
  <li>In open model, pause stops new arrivals and lets active threads finish naturally, so load drops while paused.</li>
  <li>Shutdown no longer waits for timers to complete; it waits for in-flight server responses to finish cleanly.</li>
</ul>

<h2>Parallel And Fork Controllers</h2>
<ul>
  <li>Parallel Controller runs child samplers concurrently with bounded scheduling.</li>
  <li>Fork Controller starts asynchronous child branches that share virtual-user context and complete before thread teardown.</li>
  <li>HTTP state handling was tightened to reduce unsafe sharing in concurrent paths.</li>
</ul>

<h2>AI-Assisted Scripting</h2>
<p>
BreakTest includes local agent and MCP support for AI-assisted script repair.
Tools such as Codex, Claude Code, Cursor Agent, Gemini CLI, Pi Code, OpenCode, and Copilot CLI can inspect a JMX script,
validate it, repair it, and show changes in the GUI.
</p>
<ul>
  <li>Create or repair extractors and correlations.</li>
  <li>Parameterize dynamic values.</li>
  <li>Add assertions.</li>
  <li>Fix selectors and unstable recorded data.</li>
  <li>Review generated changes from the GUI before continuing.</li>
</ul>

<h2>GUI And Large JMX Improvements</h2>
<ul>
  <li>Cleaner light/dark GUI styling.</li>
  <li>Undo and redo for semantic tree edits including add, delete, update, move, and search/replace operations.</li>
  <li>Background JMX loading with a loading overlay.</li>
  <li>Optional fast GUI loading through <code>breaktest.gui.load.fast=true</code>.</li>
  <li>Missing plugin elements load as disabled placeholders instead of failing the whole file.</li>
  <li>Search Tree can flag pre-processors, post-processors, assertions, timers, and config elements.</li>
  <li>Search results can be removed selectively with confirmation.</li>
</ul>

<h2>Visual Debugging And HAR Migration</h2>
<ul>
  <li>View Results Tree shows richer request, response, endpoint, HTTP/TLS, cookie, variable, and binary/text diagnostics.</li>
  <li>Jump-to-source support connects samples back to test elements.</li>
  <li>BreakTest JMX metadata preserves origin and source-path details.</li>
  <li>When BreakTest HAR metadata is present, recorded request/response tabs are shown in View Results Tree and HTTP sampler editors.</li>
  <li>Recorded-vs-replayed diffs help debug HAR migration and HTTP/2 replay.</li>
  <li>View Results Tree shows a transaction as soon as it starts and adds its samples below it as they complete. The transaction is updated with its timing and status when it finishes.</li>
  <li>HAR query parameters and form parameters use the HTTP form encoder to decide whether to enable URL encoding. This includes characters such as <code>@</code>, <code>/</code>, <code>:</code>, and <code>,</code>, even though some are allowed unescaped in other URL components. Imported parameter names and values remain readable; encoding is applied when sending the request. Compared with earlier imports, more parameters may have URL Encode enabled.</li>
  <li>The HAR wizard and <b>Tools &gt; Correlation Rules</b> use grouped built-in, shared custom, and plan-local Regex/JSONPath correlation rules. The Tools dialog presents a group/rule tree on the left and the selected rule's settings on the right, distinguishes fixed built-in rules from editable custom rules, and lets you enable or disable individual rules or whole groups. Use <b>Save state</b> to preserve enabled-rule choices across application updates and <b>Process rules</b> to match recorded responses in the selected Thread Group. Its Info button explains how to create a custom rule from an existing extractor.</li>
  <li>Built-in groups cover OAuth and OpenID Connect, SAML, Keycloak, Matrix, Apereo CAS, Amazon Cognito, Jenkins, Socket.IO / Engine.IO, Flask-WTF, Microsoft Entra ID, Okta, ASP.NET, JavaServer Faces, Django, Ruby on Rails, Spring Security, Laravel, CSRF meta tags, CSRF cookies and headers, Salesforce, Salesforce Commerce Cloud, SAP, ServiceNow, SharePoint, Drupal, WordPress, Magento, Liferay, Atlassian, PeopleSoft, Oracle ADF, Oracle NCA, Siebel, SignalR, and generic JSON and REST responses. Rules that read a hidden form field or meta tag accept the attributes in either order.</li>
  <li>CAS rules cover login and service tickets. Cognito rules cover challenge sessions and nested authentication tokens; cryptographic challenge responses are not generated. Jenkins rules read the crumb issuer JSON response. Engine.IO rules recognize version 4 open packets for HTTP polling; they do not implement WebSocket frame replay.</li>
  <li>CSRF cookie/header rules cover Spring and Laravel's <code>XSRF-TOKEN</code>, Django's <code>csrftoken</code>, and dedicated response headers. When a recorded request header uses the URL-decoded response token, its generated reference uses <code>__urldecode</code> to preserve that transformation during replay. Cookie headers themselves remain managed by the Cookie Manager. Rules are only proposed when an extracted value is reused by a later request; competing values and short incidental matches are rejected.</li>
  <li>Body rules are skipped for responses whose content type is an image, font, media, stylesheet, or other binary download, since those never carry correlation values and dominate a typical recording.</li>
  <li>Built-in rules are shipped in a versioned application resource and may be updated with BreakTest. Its JSON uses a <code>groups</code> array whose objects contain a group <code>name</code> and nested <code>rules</code>. Custom rules use the same structure in <code>bin/predefined-correlations.custom.json</code>, or another JSON file selected with <code>breaktest.predefined_correlations.file</code> in <code>user.properties</code>; the updater preserves this file.</li>
  <li>Each rule has a <code>maxMatches</code> safety limit that defaults to <code>1</code>. BreakTest ignores an expression when its response match count exceeds that limit. Within the limit, it compares every extracted candidate with later requests and sets the generated extractor's match number to the occurrence that produced the used value; the match number is not predefined in the catalog.</li>
  <li>In <b>Tools &gt; Correlation Rules</b>, use <b>Export custom rules...</b> to share all custom rules currently available to the dialog as a standalone JSON file. Built-in rules are excluded and their IDs cannot be overwritten by imported custom rules. Export asks before replacing an existing file. On another installation, use <b>Import custom rules...</b> to merge the file into the installation catalog and current JMX archive; matching rule IDs are replaced and unrelated rules are retained. Save the test plan to persist its updated archive.</li>
  <li><b>Show custom rules only</b> filters the rule browser without changing which rules are enabled for matching. Importing the same file again updates matching IDs rather than adding duplicate rules.</li>
  <li>Right-click a Regex or JSONPath Extractor and choose <b>Save as Custom Predefined Correlation</b>, then select an existing custom group or enter a new group name (leave empty for <b>Custom</b>). BreakTest stores the rule in both the custom installation catalog and <code>correlations/custom-predefined-rules.json</code> inside the JMX archive, making it available locally and shippable with the plan.</li>
</ul>

<h2>Scripting, Data, And Assertions</h2>
<ul>
  <li>If and While Controllers support structured condition rows with all/any matching while preserving legacy expressions.</li>
  <li>While Controllers have an optional <code>Max iterations</code> field. Leave it blank for no limit,
  or enter a non-negative integer (including a variable or function resolving to one). Zero skips the loop.
  The cap applies alongside either condition mode and counts complete iterations independently of the
  displayed index starting at zero or one. The count resets each time the controller is entered from its parent.
  Invalid or negative limits stop the loop and log an error.</li>
  <li>Loop and while index behavior options are visible and configurable.</li>
  <li>Transaction Controller includes measurement modes plus delay and pacing controls.</li>
  <li>Transaction Controller has no <b>Generate parent sample</b> option. Every sample is sent to listeners as soon as it completes and links to the transaction it ran in, and the transaction sample follows when the transaction ends. Transactions only keep running totals, never the samples themselves. Plans saved with the old option load unchanged, and the option is not saved again.</li>
  <li>Flat result listeners show top-level transactions and samples outside transactions by default. Enable <b>Children</b> on a listener to include child samples and nested transactions in its display and statistics. This applies to live results and loaded result files with transaction IDs; saved result files still contain all samples allowed by the logging filters. Clear the display before a new run, or reload the file, after changing the option. View Results Tree always keeps children expandable under their transaction.</li>
  <li>Boundary, CSS/HTML, Regex, XPath, XPath2, JSONPath, and JMESPath extractors support fail-on-no-match behavior.</li>
  <li>CSV Data Set can read records in random order.</li>
  <li>CSV preview shows first sample variable assignments.</li>
</ul>

<h2>Native UDP</h2>
<ul>
  <li>UDP Request sends datagrams with optional response waiting, response timeout, local address and port binding, and socket reuse.</li>
  <li>UDP Receiver can read a later datagram from a named socket created by UDP Request.</li>
  <li>A friendly payload-format selector provides UTF-8 text, hexadecimal, and single-byte text; advanced custom codecs can implement the native <code>UDPTrafficCodec</code> interface.</li>
  <li>The implementation uses the Java networking API directly and does not require the JMeter Plugins common I/O or common JMeter libraries.</li>
</ul>
<p>See <a href="udp-request.html">Native UDP Request And Receiver</a> for configuration details.</p>

<h2>Removed Or Changed Compatibility Areas</h2>
<ul>
  <li>RMI Remote Server style distributed execution is removed. Use BreakTest Enterprise or another controlled orchestration layer for distributed runs.</li>
  <li>BreakTest-specific JMX metadata may not round-trip into older Apache JMeter versions.</li>
  <li>Plugins that depend on removed internals may require migration.</li>
  <li>HttpClient 4 runtime jars remain available for legacy plugin compatibility, but the built-in HTTP Request sampler uses HttpClient 5.</li>
</ul>

</section>
<section name="Ignoring sampler results">
<p>The result inclusion menu beside Comments in the shared sampler header offers three modes.
Its button displays the selected mode:</p>
<ul>
<li><code>Included</code> (default): report results normally.</li>
<li><code>Ignore on success</code>: report only failed results, including assertion failures.</li>
<li><code>Ignore always</code>: exclude all results from this sampler.</li>
</ul>
<p>The sampler still runs, along with its post-processors and assertions. Ignored results are excluded
from listeners and the transaction's sample count, bytes, latency, connect time and failure status,
using the same behavior as <code>prev.setIgnore()</code>.</p>
<p>Transaction elapsed time still includes the ignored request's wait in both
<code>Total run time including timers</code> (the default) and <code>Total run time excluding timers</code>.
Use <code>Sum child sampler times</code> to leave ignored sampler durations out of the transaction time.
This mode sums included child durations rather than measuring wall-clock time, so parallel requests
can produce a sum longer than the transaction's actual duration.</p>
<p>With <code>Ignore always</code>, a failed request does not mark its enclosing transaction as failed:
the transaction can report success even though that request failed. The thread's last-sample status,
on-error stop actions and logical actions still apply. Scripts that explicitly ignore a result
continue to work in every mode.</p>
<p>The selection is saved with the sampler in the test plan and also applies in command-line runs.
The control is shared by standard sampler editors and TestBean sampler editors; plugins with fully
custom headers may need to adopt the shared title panel to display it. Plugin editors must call
<code>super.configure(element)</code> when loading a sampler; otherwise, saving through
<code>configureTestElement</code> can write an Ignore setting left over from another sampler.</p>
</section>
</body>
</document>
